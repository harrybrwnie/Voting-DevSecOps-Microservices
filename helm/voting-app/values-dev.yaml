namespace: voting-dev

imagePullSecrets: []

image:
  tag: "latest"

vote:
  replicaCount: 1
  service:
    type: LoadBalancer

result:
  replicaCount: 1
  service:
    type: LoadBalancer

worker:
  replicaCount: 1

postgres:
  storage: 1Gi
  storageClassName: gp2
  database: postgres
  username: postgres
  password: postgres