namespace: voting-dev

imagePullSecrets: []

image:
  tag: "latest"

vote:
  replicaCount: 1

result:
  replicaCount: 1

worker:
  replicaCount: 1

postgres:
  storage: 1Gi
  storageClassName: gp2
  database: postgres
  username: postgres
  password: postgres
